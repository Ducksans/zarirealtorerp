# ERP System Architecture & Business Rules (SSOT)

이 문서는 부동산 중개법인 ERP 시스템의 단일 진실 공급원(Single Source of Truth, SSOT)입니다. 
AI 에이전트와 모든 개발자는 코드 작성 전 **반드시 이 문서를 참조**하여 할루시네이션 및 아키텍처 드리프트를 방지해야 합니다.

## 1. 개발 대원칙 (Grand Principles)
1. **드리프트 및 할루시네이션 방지:** 모든 비즈니스 로직과 데이터 검증은 이 문서의 룰을 최우선으로 따릅니다. 코드를 작성할 때는 `Zod` 등을 통해 런타임 검증을 실시하며, 임의로 필드나 로직을 지어내지 않습니다.
2. **1파일 1책임주의 (SRP):** 프론트엔드의 UI 컴포넌트, 백엔드의 Controller(Route), Service(비즈니스 로직), Middleware는 각각 하나의 목적만을 달성하기 위해 독립적인 파일로 분리되어야 합니다.
3. **명시적 의존성 및 목적 (JSDoc):** 모든 소스코드 파일의 최상단에는 해당 파일의 생성 목적, 의존성, 작성 목적을 나타내는 JSDoc 헤더 태그가 명시되어야 합니다.
4. **무결성(Integrity) 지향:** API 실패 시 조용히 넘어가거나 성공 알림을 띄우지 않습니다. 에러는 반드시 로깅되고 사용자에게 명확히 전달되어야 하며, 다량의 DB Insert/Update는 트랜잭션으로 보호되어야 합니다.

---

## 2. 핵심 비즈니스 로직: SSOT 100원 Rule (정산 비율)
회사의 모든 정산은 **회사 매출(Gross)이 100원 발생했을 때 이를 어떻게 분배하는가**에 대한 절대 규칙을 따릅니다.
단, 어떠한 경우에도 전체 수당 지급액이 회사 매출(Gross)을 초과할 수 없습니다.

### 2.1 개인 영업 수당 (Base Pay)
- **평사원 (REGULAR):** 100원 중 50원 (50%)
- **팀장 (TEAM_LEADER):** 100원 중 60원 (60%)
- **본부장 (DIV_HEAD) / 지점장 (BRANCH_MGR):** 100원 중 70원 (70%)

### 2.2 영업지원비 (Bonus Pay)
본사 입금액(회사 수익)을 기준으로 개인의 영업지원비를 추가 지급합니다.
(본부장, 지점장은 자신의 개인 영업에 대한 영업지원비를 받지 않습니다.)
- **회사 입금액 100만 원 미만:** 0%
- **회사 입금액 100만 ~ 1000만 원 미만:** 10% (단, 100만 원 단위로 계단식 절사 적용)
- **회사 입금액 1000만 원 이상:** 15% (계단식 절사 없음)

### 2.3 오버라이딩 (Override Pay - 상향 전파)
하급자의 회사 입금액에서 상급자에게 지급되는 관리 수당입니다.
- **사원 -> 직속 팀장:** 사원 회사 입금액의 20%
- **본부장 오버라이딩:** 산하 본부 소속의 팀장+사원 전체 회사 입금액의 10%
- **지점장 오버라이딩 풀(Pool):** 모든 사원 및 팀장의 회사 입금액 중 5%를 지점장 풀로 적립한 뒤, 지점장 수(N)로 1/n 분배.

---

## 3. 기술 스택 (Tech Stack)
- **Frontend:** Next.js 14+ (App Router), React, Tailwind CSS
- **Backend:** Next.js Route Handlers, Prisma ORM
- **Database:** SQLite (개발 환경) -> 향후 PostgreSQL 확장 고려
- **Validation:** Zod (도입 예정)

## 4. 디렉토리 구조 전략
```text
src/
 ├── app/              # Next.js App Router (페이지 및 API 엔드포인트)
 │    ├── api/         # Controller 계층 (요청 파싱, 응답 포맷팅, Zod 검증)
 │    └── ...          # Page 컴포넌트 계층
 ├── components/       # 재사용 가능한 UI 컴포넌트 (1파일 1책임)
 ├── services/         # 비즈니스 로직 및 DB 접근 계층 (Service Layer)
 ├── lib/              # 공통 유틸리티 (에러 핸들러, DB 클라이언트 등)
 └── types/            # 공유 타입 정의
```
